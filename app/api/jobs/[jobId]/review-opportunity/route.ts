import { z } from "zod";
import { resolveTenantContext,requirePermission } from "@/lib/auth/context";
import { createSupabaseAdminClient } from "@/lib/supabase/admin";
import { parseJson } from "@/lib/api/request";
import { jsonError,ApiError } from "@/lib/api/errors";
import { createManualPackage } from "@/lib/manual/package-service";
const schema=z.object({agencyId:z.string().uuid(),clientId:z.string().uuid(),projectId:z.string().uuid(),decision:z.enum(["proceed","reject"])});
export async function POST(request:Request,{params}:{params:Promise<{jobId:string}>}){try{const input=await parseJson(request,schema),context=await resolveTenantContext({agencyId:input.agencyId,clientId:input.clientId,projectId:input.projectId,requireProject:true});requirePermission(context,"draft.approve");const{jobId}=await params,db=createSupabaseAdminClient();if(!db||!context.project||!context.client)throw new ApiError("Supabase is not configured.",503,"NOT_CONFIGURED");const job=await db.from("seo_campaign_jobs").select("status,result").eq("id",jobId).eq("agency_id",context.agency.id).eq("project_id",context.project.id).maybeSingle();if(!job.data||job.data.status!=="awaiting_opportunity_review")throw new ApiError("This job is not awaiting opportunity review.",409,"CONFLICT");const opportunityId=job.data.result?.opportunityId as string|undefined,draftId=job.data.result?.draftId as string|undefined;if(!opportunityId||!draftId)throw new ApiError("The selected draft is unavailable.",409,"CONFLICT");if(input.decision==="proceed"){const draft=await db.from("seo_action_drafts").select("execution_path").eq("id",draftId).eq("project_id",context.project.id).single();if(draft.data?.execution_path==="repository"){requirePermission(context,"execution.approve");const gate=await db.rpc("github_execution_readiness",{target_agency:context.agency.id,target_project:context.project.id});if(!gate.data?.ready)throw new ApiError(`Repository execution is blocked: ${(gate.data?.blockers??[]).join(", ")}.`,409,"CONFLICT");await db.from("seo_campaign_jobs").update({status:"queued",current_stage:"inspect_repository",next_attempt_at:new Date().toISOString()}).eq("id",jobId);await db.from("seo_opportunities").update({status:"approved"}).eq("id",opportunityId);return Response.json({ok:true,decision:input.decision,implementationPath:"repository"});}const created=await createManualPackage(db,{agencyId:context.agency.id,clientId:context.client.id,projectId:context.project.id,opportunityId,draftId,createdBy:context.user.id,requestedPath:draft.data?.execution_path});await Promise.all([db.from("seo_campaign_jobs").update({status:"awaiting_manual_completion",progress_percent:75,result:{...job.data.result,packageId:created.id,taskId:created.taskId}}).eq("id",jobId),db.from("seo_opportunities").update({status:"in_progress"}).eq("id",opportunityId)]);return Response.json({ok:true,decision:input.decision,implementationPath:created.implementation_path,packageId:created.id,taskId:created.taskId});}await Promise.all([db.from("seo_campaign_jobs").update({status:"cancelled"}).eq("id",jobId),db.from("seo_opportunities").update({status:"dismissed"}).eq("id",opportunityId)]);return Response.json({ok:true,decision:input.decision});}catch(error){return jsonError(error)}}
