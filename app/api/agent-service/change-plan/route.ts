import {z} from "zod";import {jsonError} from "@/lib/api/errors";import {parseJson} from "@/lib/api/request";import {requireLiveAgencyProject} from "@/lib/auth/live-tenant";import {agentServicePlans} from "@/lib/agent-service/catalog";import {agentServiceSnapshot,changeAgentServicePlan} from "@/lib/agent-service/service";
const plans=Object.keys(agentServicePlans) as [keyof typeof agentServicePlans,...Array<keyof typeof agentServicePlans>],schema=z.object({projectId:z.string().uuid(),planKey:z.enum(plans)});
export async function POST(request:Request){try{const input=await parseJson(request,schema),context=await requireLiveAgencyProject({projectId:input.projectId,permission:"billing.manage"});await changeAgentServicePlan(context.db,{agencyId:context.agencyId,clientId:context.clientId,projectId:input.projectId,userId:context.userId},input.planKey);return Response.json({ok:true,service:await agentServiceSnapshot(context.db,{agencyId:context.agencyId,clientId:context.clientId,projectId:input.projectId})});}catch(error){return jsonError(error);}}
